#pragma once
#include "Set.h"
/*class map :
    public set
{
	struct pair
	{
		elem key;
		double value;
	};
	//Список элементов (указатель на первый элемент)
	pair* first_el = nullptr;
	//Указатель на последний элемент списка
	pair* last_el = nullptr;
};

*/